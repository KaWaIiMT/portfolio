# 作品集已部署

**网址: https://kawaiimt.github.io/portfolio/**

## 每次更新流程

```powershell
cd "D:\_College\保研\作品集"
npx next build
git add -A
git commit -m "描述你的改动"
git push
```

GitHub Pages 收到 push 后自动从 docs/ 部署，1-2 分钟生效。

## 代理

git 已配好走 Clash (127.0.0.1:7897)。如果端口变了：
```powershell
git config --global http.proxy "http://127.0.0.1:新端口"
git config --global https.proxy "http://127.0.0.1:新端口"
```
